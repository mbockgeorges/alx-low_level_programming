#include "main.h"

/**
 * largest_number - print largest number
 * @a: first integer
 * @b: second integer
 * @c: third integer
 * Return: largest number
 */

int largest_number(int a, int b, int c)
{
	int largest = a;

	if (a > b && a > c)
	{
		largest = a;
		return (largest);
	}
	else if (b > a && b > c)
	{
		largest = d;
		return (largest);
	}
	else
	{
		largest = c;
		return (largest);
	}

	return (largest);
}
