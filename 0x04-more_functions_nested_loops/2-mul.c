#include "main.h"

/**
 * mul - function
 * @a: integer
 * @b: integer
 * Description: it's a function that multiplies two integers
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	_putchar("%d", a * b);

	return (0);
}
